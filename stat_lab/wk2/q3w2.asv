%% a)
f = @(x) 1/46 * exp(-x ./ 46);
x = linspace(0, 200, 10001);

figure(1);
plot(x, f(x))

%% d)
figure(2);
data = exprnd(46, [1, 1000]);
histogram(data, 'Normalization', 'pdf' )

%The density histogram closely follows the curve in part a)

%% iii)
x_mean = mean(data)
%The mean for the vector is very close to 